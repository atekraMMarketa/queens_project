.headers
  %h2= @product.name

%h4="Popis produktu:"

= @product.description

.pull-right
  = image_tag @product.picture(:medium)

%h4="Dostupné varianty:"

- @product.variants.each do |variant|
  .variant-box
    = variant.name

